/* libhyle — main source anchor.
 * Core implementations live in ctx.c, value.c, etc. */
/* corm auto-initializes via constructor attribute, nothing to init here */
typedef int hyle_anchor;  /* suppress -Wpedantic empty translation unit */
